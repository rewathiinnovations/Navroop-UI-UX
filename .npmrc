# Install-script policy lives in pnpm-workspace.yaml (`allowBuilds`): only the
# packages named there may run pre/postinstall scripts. Never set
# `dangerouslyAllowAllBuilds=true` here — it voids that allowlist for every
# transitive dependency on CI and on every developer machine (F-700/F-635).
