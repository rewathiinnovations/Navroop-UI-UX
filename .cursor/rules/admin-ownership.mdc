---
description: Prefer one owner for overlapping admin team/usage/invite files
globs: "{app/admin,app/api/admin,components/app/studio}/**/*.{ts,tsx}"
alwaysApply: false
---

# Admin file ownership

Admin UI and APIs overlap (`/admin/team`, `/admin/usage`, `/api/admin/invite`, `/api/admin/team`, `/api/admin/usage`, plus shared studio chrome). Prefer **one owner** per change set.

- One agent or PR should own a given admin file. Do not split the same page or route across parallel edits.
- Shared chrome (`StudioShell`, `PageTabs`, `UserMenu` admin link) is the studio owner’s file. Admin pages import it; they do not fork it.
- Team invite flow: UI in `app/admin/team/page.tsx`, API in `app/api/admin/invite/route.ts`. Change both in the same pass if the contract changes.
- Guard admin routes with `user.role === "ADMIN"`. Do not weaken invite-only access.
